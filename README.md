## Linux Deployment

??????????? Debian/Ubuntu ? Linux ???????????????`traceroute` ? ZMap ????????? `sudo` ??????

### 1. ????

```bash
git clone <your-repo-url>
cd pressure_test-change
```

### 2. ??????

```bash
sudo apt update
sudo apt install -y   python3 python3-venv python3-dev python3-pip   build-essential cmake pkg-config   libjson-c-dev libpcap-dev libgmp-dev libunistring-dev   gengetopt flex byacc traceroute curl git
```

### 3. ????????? Python ??

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. ?? ZMap ??????

```bash
chmod +x build_zmap.sh
sudo ./build_zmap.sh
```

?????????????????????

- ?????????? `#include` ?? `zbopt.h` / `topt.h` ????
- GCC 15 ? `source_ip_addresses = NULL` ???????????

?????

- ???????????????????????????
- ????????? `build` / `CMakeCache.txt` / `CMakeFiles` ???
- ?????? `single` ? `multiple probes` ?? `zmap` ????????????????

### 5. ?? GeoLite2 ???

???????

```text
attack_resources/tcp/resources/geoip/GeoLite2-City.mmdb
```

????????? MaxMind ?? `GeoLite2-City.mmdb` ??????????????????????????????

### 6. ????

```bash
chmod +x start.sh
sudo ./start.sh
```

`start.sh` ?????????????? `venv`????????????????????????????????? Python ???

????????

```text
http://localhost:5000
```

## Shared IP Resource Model

???????????? IP ????

```text
attack_resources/shared/ip_lists
```

???????

- ????????????????? IP ???
- ????????????????????????????????
- ????????????? `tcp`?`dns`?`memcached`?`ntp` ?????????
- ?????????????????????????????????
- ??????????????????????????????

## Troubleshooting

### `zbopt.h: No such file or directory` / `topt.h: No such file or directory`

?????????????? `#include` ???????????

```bash
sudo ./build_zmap.sh
```

????????? `#include` ??????????

### `source_ip_addresses = NULL` ? GCC 15 ?????

?????????????????????? `build_zmap.sh` ??????????????????

### ?????????????????

?? `CMakeCache.txt` / `CMakeFiles` ?????????????????????????????????????????????????????????

```text
vendor/weaponizing-censors/zmap/build
vendor/weaponizing-censors/zmap/CMakeCache.txt
vendor/weaponizing-censors/zmap/CMakeFiles
vendor/weaponizing-censors/zmap/src/CMakeFiles
vendor/weaponizing-censors/zmap_multiple_probes/build
vendor/weaponizing-censors/zmap_multiple_probes/CMakeCache.txt
vendor/weaponizing-censors/zmap_multiple_probes/CMakeFiles
vendor/weaponizing-censors/zmap_multiple_probes/src/CMakeFiles
```

### `start.sh` ?????????

??????????

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
sudo ./start.sh
```

### ??????????????? TXT

????????????????????????????

```text
attack_resources/shared/ip_lists
```

????????????????????????????

## Notes

- ???????????? `requirments.txt` ??????????? `requirements.txt`?
- ?????? Linux ?????????????? Windows ????????????? Linux ????????????
